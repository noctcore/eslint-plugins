import { makeCreateRule } from '@noctcore/eslint-utils';

/** RuleCreator for this plugin; docs URLs resolve under `eslint-plugin-security/docs/rules`. */
export const createRule = makeCreateRule('security');
