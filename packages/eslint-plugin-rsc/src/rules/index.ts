import { noNavigationThrowInTryRule } from './no-navigation-throw-in-try';

/** Every rule this plugin exposes, keyed by its (unprefixed) rule id. */
export const rules = {
  'no-navigation-throw-in-try': noNavigationThrowInTryRule,
};
