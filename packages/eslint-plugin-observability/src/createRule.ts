import { makeCreateRule } from '@noctcore/eslint-utils';

/** RuleCreator for this plugin; docs URLs resolve under `eslint-plugin-observability/docs/rules`. */
export const createRule = makeCreateRule('observability');
