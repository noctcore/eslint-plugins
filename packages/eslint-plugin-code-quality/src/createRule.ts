import { makeCreateRule } from '@noctcore/eslint-utils';

/** RuleCreator for this plugin; docs URLs resolve under `eslint-plugin-code-quality/docs/rules`. */
export const createRule = makeCreateRule('code-quality');
